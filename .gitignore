__pycache__
node_modules
dist
dist-lib
build
*.log
*.egg-info/
.venv/
# Built SPA lands in the package at ship time (source of truth is ui/).
yz_head/static/
# NOTE: yz_head/assets/ IS tracked — it holds the bundled default mesh
# (face-model.obj) + default lip/eye calibration that ship in the wheel.
